---
type: plan
vault: spec-first-kit
spec:                 # 짝이 되는 SPECIFY 문서: "[[프로젝트명_SPEC]]"
---

# PLAN — 어떻게 구현하나

> **④ Plan** 층 — 구현자가 신경 쓰는 것. `SPECIFY.md`(무엇/왜)가 확정된 뒤 작성한다.
> 여기서 정한 구조 결정은 헌법(`CONSTITUTION.md` §2 경계)이 강제한다.

---

## 1. 기술 스택

- 언어/런타임: 
- 핵심 라이브러리: 
- 실행 형태: `로컬 웹페이지 / CLI / 스크립트 / 데스크탑`
- 선택 이유 (한 줄): 

---

## 🚧 헌법 검사 ① — 설계 전 (구조 확정 전 통과 필수)

> `CONSTITUTION.md`를 어기는 설계는 **시작도 하지 않는다.** 통과 못 하면 멈추고 다시.
> 아래 ②와 쌍을 이룬다 (Spec Kit의 Constitution Check ×2).

- [ ] ⚠️ **보안·신뢰경계** — 실행을 결정하는 입력(CLI경로·명령·env)을 신뢰 안 된 출처(`.env`·외부파일·clone 저장소)에서 받지 않는가?
- [ ] ⚠️ **권한** — 도구·MCP는 최소·읽기전용 우선인가? 비가역·외부부작용은 사람 승인인가? (allowlist)
- [ ] **§2 경계** — 의존이 한 방향인가? 순환 가능성이 구조에 없는가?
- [ ] **§3 단순성** — 최소 구조인가? 불필요한 추상화·래퍼·갓파일 조짐이 없는가?
- [ ] **§1 중복** — 기존 자산(다른 산출물·라이브러리)에 같은 게 없는지 먼저 검색했는가?

---

## 2. 아키텍처 · 모듈 경계

<!-- 의존은 한 방향. 순환 금지. SSOT 지정. (CONSTITUTION §1·§2와 연결) -->

**모듈 분할:**
- 
- 

**의존 방향:**  
`____ → ____ → ____`  (한 방향)

**SSOT (단일 출처):**  
- 공용 타입/shape 정의 위치: 

---

## 3. 데이터 구조

- 저장 필요: `예 / 아니오`
- 저장 위치: `로컬 파일 / localStorage / DB / 없음`
- 저장 형식: `JSON / CSV / 텍스트`

**핵심 엔티티 (shape):**
```
entity = {
  
}
```

---

## 4. 화면 구조

<!-- 화면별 목적 + 필수 요소 -->

- 
- 

---

## 5. 처리 흐름 (시스템 관점)

<!-- SPECIFY의 사용흐름이 내부에서 어떻게 처리되는가 -->

`입력 → ① ___ → ② ___ → ③ ___ → 출력`

---

## 6. 구현 순서

<!-- 핵심 사용 흐름을 가능하게 하는 최소 단위부터 -->

1. 
2. 
3. 

---

## 🚧 헌법 검사 ② — 구현 진입 전 (설계 완료 후 재검사)

> 설계를 다 짠 뒤 **다시** 본다. ①과 ② 사이에 늘어난 복잡도·결합을 여기서 잡는다.
> 모두 통과해야 코드 작성 시작.

- [ ] **§1 SSOT** 지정됨 (공용 타입/shape 정의 위치)
- [ ] **§2 경계** 모듈 경계·의존 방향 확정, 숨은 결합(전역상태·초기화순서·side-effect) 없음
- [ ] **§3 복잡도** 갓펑션·갓파일 안 나오는 분할인가? (라인 아닌 복잡도 기준)
- [ ] **§4 에러** 에러 경계를 한 곳으로 정했는가? (fail loud)
- [ ] **§5 타입** 전략 정해짐 (strict / JS면 JSDoc)
- [ ] **§6 테스트** 대상·엣지케이스 목록 있음 (빈입력·null·경계값·실패경로 — 구현 전 작성)
- [ ] ⚠️ **보안** 항목(①) 재확인

**위반 정당화** — 어긴 항목이 있으면 여기 적는다 (없으면 비움):

| 어긴 규칙 | 왜 어겼나 | 더 단순한 대안을 왜 버렸나 |
|---|---|---|
| | | |

> 표가 비어야 정상이다. 채워야 한다면 — 정말 어쩔 수 없는지 한 번 더 의심하라.
